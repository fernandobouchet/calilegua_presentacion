const Footer = () => {
  return (
    <footer className="w-full relative bottom-0 text-xs lg:text-sm flex justify-center">
      <span> © {new Date().getFullYear()} Fernando Bouchet</span>
    </footer>
  );
};

export default Footer;
